import 'dart:io';

void main() {
  int carModel = 2018;
  assert(carModel != 2014,
      "car model must be subaru!"); //assert method is used to check for errors and it uses conditions as an argument
}
